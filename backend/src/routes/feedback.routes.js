import {Router} from 'express';import {authenticate,authorize} from '../middleware/auth.js';import {getTemplates,createTemplate,deleteTemplate,getFeedback,createFeedback,deleteFeedback} from '../controllers/feedback.controller.js';
const r=Router();r.use(authenticate);r.get('/templates',getTemplates);r.post('/templates',authorize('admin','teacher'),createTemplate);r.delete('/templates/:id',authorize('admin','teacher'),deleteTemplate);r.get('/',getFeedback);r.post('/',authorize('admin','teacher'),createFeedback);r.delete('/:id',authorize('admin','teacher'),deleteFeedback);export default r;
