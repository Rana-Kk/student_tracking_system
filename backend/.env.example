# Copy this file to .env and fill in real values. Never commit .env.

PORT=4000

DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=lexicon_learning_analytics

JWT_SECRET=change-this-to-a-long-random-string
JWT_EXPIRES_IN=8h

# Used later by the GitHub polling job (Faz 2)
GITHUB_TOKEN=
OPENAI_API_KEY=
